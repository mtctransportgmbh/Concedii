rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Fiecare utilizator poate accesa doar propriile date
    match /concedii/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
